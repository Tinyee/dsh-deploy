# ============================================================
# setup-profile.plugins 示例 —— 复制为 $DSH_HOME/setup-profile.plugins
# 后由 scripts/setup-profile.sh 读取，用 dsh plugin 正式安装。
#
# 规则: 每行一个 npm 包名；# 开头为注释；空行忽略。
# 说明: dshmarket（插件市场）始终安装，无需写在这里。
# ============================================================

# Tailscale 远程访问运营面板（健康检查/HTTPS 入口/ACL 等）
# dsh-tailscale-console

# DSH 移动端适配与安全访问（局域网/远程/Android App/手机浏览器）
# dsh-mobile

# DSH Remote 官方 bundle（左侧边栏 + 右侧控制台 + /fs/* 文件传输，
# 配合 Android App 远程操控会话/审批/goal）
# dsh-remote-plugin
